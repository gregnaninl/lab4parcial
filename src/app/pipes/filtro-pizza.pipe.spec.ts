import { FiltroPizzaPipe } from './filtro-pizza.pipe';

describe('FiltroPizzaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPizzaPipe();
    expect(pipe).toBeTruthy();
  });
});
